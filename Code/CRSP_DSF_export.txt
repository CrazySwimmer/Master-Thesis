PROC SQL;

CREATE TABLE CRSP_DSF AS
	SELECT *
	FROM CRSP.DSF
	WHERE DATE < '01SEP2019'd
	AND DATE >= '01JAN2000'd
	AND PERMNO IN (
		SELECT PERMNO
		FROM CRSP.DSP500LIST
		WHERE start < '01SEP2019'd
		AND ending >= '01JAN2001'd
	);

RUN;
QUIT;